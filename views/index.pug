extends layout

      
block content

  script(src="https://unpkg.com/axios/dist/axios.min.js")

  script.
    function scheduleMessage(date, message){
      axios.post('/send', null)
        .then(function (response) {
          console.log(response);
        })
        .catch(function (error) {
          console.log(error);
        });

    }
  h1= title
  p Welcome to #{title}
  button(onclick='scheduleMessage()') Send message to Jack in one minute
